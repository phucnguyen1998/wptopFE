import ErrorMessage from "../components/common/ErrorMessage";

export default {
  title: 'Components/Error Message',
  component: ErrorMessage
}

export const Error = () => <ErrorMessage>Error</ErrorMessage>